# Interaction model

The platform is organized around governed signals rather than passive dashboards:

```text
Signal → Context → Explanation → Human decision → Action → Follow-up → Measured value
```

Every state-changing action must show the object affected, actor, timestamp, reason, and resulting status.
