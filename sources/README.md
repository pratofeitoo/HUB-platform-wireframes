# Sources

This folder contains the relationship between the standalone design workspace and the canonical HUB source material.

- `source-snapshots/` contains verbatim copies.
- `extracted-data/` contains structured derived data.
- `transformations/` documents extraction and normalization.
- `source-register.md` records provenance and authority.
