# Extraction notes

## Current status

The source workbook and Markdown specification are copied verbatim in `sources/source-snapshots/`. Structured JSON extraction is not yet performed.

## Planned extraction

Extract the 15 workbook sheets into named JSON files while preserving IDs, original labels, Portuguese text, source sheet, extraction date, and row-level provenance.
