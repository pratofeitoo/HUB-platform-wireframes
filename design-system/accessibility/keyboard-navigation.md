# Keyboard navigation

All primary actions, filters, tables, alerts, dialogs, and audit timelines must have a visible and logical keyboard order. Focus must not be trapped except inside an active modal.
