# Prototype source

The implementation will be organized by role-aware layouts, reusable components, screen routes, synthetic data, and shared styles. Keep prototype-only behavior clearly separated from production assumptions.
